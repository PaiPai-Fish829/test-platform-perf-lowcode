# 一般由 scripts/sync_platform_env.py 根据 locust-config.yaml 自动生成；也可手动覆盖
# VITE_LOCUST_URL=http://localhost:8089

# 可选：直接指定 API 基址（生产环境若不使用 /locust-api 代理则设为 Locust 地址）
# VITE_LOCUST_API_BASE=http://localhost:8089

# Grafana 仪表盘地址（后续可嵌入服务器性能 Tab）
VITE_GRAFANA_URL=http://localhost:3000
