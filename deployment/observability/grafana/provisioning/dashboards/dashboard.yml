apiVersion: 1

providers:
  - name: "locust-dashboard"
    orgId: 1
    folder: "Locust"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
      # 推荐 locust-variable.json（变量化）；locust-overview.json 为固定 job 参考
